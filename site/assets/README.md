# assets/

Drop the presentation file here before packaging the standalone site zip:

```
Pulse_Guardian_ICU_Presentation_Hebrew.pptx
```

It's intentionally not committed to git (it's a ~24MB binary and doesn't belong in version
control history), but `index.htm` already links to it at that exact path under the
"Downloads & Links" section, so the site works as soon as the file is placed here.
